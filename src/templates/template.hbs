<!DOCTYPE html>
<html>
  <head>
    <title>PDF</title>
  </head>
  <body>
    <div style="text-align: center">
      <h1>Interactive Brokers Analyzer</h1>
      <h3>Export at {{date}}</h3>
    </div>
    {{#with table}}
    <h2>{{title}}</h2>
    <table>
      <thead>
        <tr>
          {{#each rows.[0]}}
          <th>{{@key}}</th>
          {{/each}}
        </tr>
      </thead>
      <tbody>
        {{#each rows}}
        <tr>
          {{#each this}}
          <td>{{this}}</td>
          {{/each}}
        </tr>
        {{/each}}
      </tbody>
    </table>
    {{/with}}
  </body>
</html>